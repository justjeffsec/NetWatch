# Ensure shell scripts always use Unix line endings (LF)
# even when checked out on Windows — Docker requires this
*.sh text eol=lf
entrypoint.sh text eol=lf
Dockerfile text eol=lf
monitor/Dockerfile text eol=lf
